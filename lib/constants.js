const SK = 0x00;
const VALUE = 0x01;
const SND = 0x02;
const SHARD_ID = 0x03;
const RAND = 0x04;
const FULL = 0x05;

const COMPRESS_POINT_SIZE = 33;
const BIG_INT_SIZE = 32;

const UINT64_SIZE = 8; // bytes
const PC_CAPACITY = 5;

module.exports = {
    COMPRESS_POINT_SIZE,
    BIG_INT_SIZE,
    PC_CAPACITY,
    SK,
    VALUE,
    SND,
    SHARD_ID,
    RAND,
    FULL,
    UINT64_SIZE,
};